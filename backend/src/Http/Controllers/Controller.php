<?php
declare(strict_types=1);

namespace CallMetrics\Http\Controllers;

/**
 * Clase Controller
 *
 * Controlador base abstracto del que extienden todos los controladores de la aplicación.
 * Proporciona un punto de extensión común para toda la capa HTTP.
 *
 * @description Controlador raíz del patrón MVC. Todos los controladores específicos
 *              heredan de esta clase para mantener consistencia en la arquitectura.
 * @package CallMetrics\Http\Controllers
 */
abstract class Controller
{
    // Controlador base — todos los controladores extienden de este.
}
